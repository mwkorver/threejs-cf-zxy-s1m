{
  "name": "flight-sim-client",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Flat-Mercator flight-sim client (FLIGHT-SIM-PLAN.md §5). Engine spike pending (§10.2): src/core is engine-agnostic; spikes live in src/spikes/.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "test": "vitest run"
  },
  "devDependencies": {
    "@deck.gl/core": "^9.0.0",
    "@deck.gl/mesh-layers": "^9.0.0",
    "@luma.gl/core": "^9.0.0",
    "@luma.gl/engine": "^9.0.0",
    "@luma.gl/webgl": "^9.0.0",
    "@math.gl/core": "^4.0.0",
    "@types/three": "^0.171.0",
    "three": "^0.171.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
